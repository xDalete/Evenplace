/* TODO: Ver se com o tempo que nos resta vamos conseguir implementar essa bomba de componente 
import React from "react";
import styles from "./SeatLegend.module.scss";

export const SeatLegend: React.FC = () => {
  return (
    <div className={styles.container}>
      <div className={styles.item}>
        <div className={`${styles.indicator} ${styles.paid}`} />
        <span>Vagas Pagas</span>
      </div>
      <div className={styles.item}>
        <div className={`${styles.indicator} ${styles.reserved}`} />
        <span>Vagas Reservadas</span>
      </div>
      <div className={styles.item}>
        <div className={`${styles.indicator} ${styles.available}`} />
        <span>Disponível</span>
      </div>
    </div>
  );
};
*/
